# Grid Toolkit (placeholder)

Toolkit agnóstico para grid — será substituído por addon real (MIT) quando necessário.

Funções atuais já em `systems/tactical/grid_system.gd` (floodfill, world_to_cell).

Futuro: `addons/grid_toolkit` com `GridUtility`, `GridPathfinding` (A*), `GridData` separados, cada um com `plugin.cfg`.
