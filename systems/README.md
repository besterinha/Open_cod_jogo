# Systems

Regras do jogo, usam `addons/`, nunca dependem de `content/` ou `gyms/`.

- `common/` — EventBus, Registry, SaveSystem, DataValidator
- `caravan/` — CaravanManager, EventResource, EventChoice
- `tactical/` — GridSystem
- `ability/` — AbilityResource, IAbilityLogic, logics/
