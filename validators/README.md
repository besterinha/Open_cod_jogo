# validators/

**Daily config:** `ProjectValidationRules.tres` (`VFProjectValidationConfig`)  
**Profiles:** `profiles/*.tres` (`VFValidationProfile`) — what to scan  

Advanced script-backed definitions only via Project Settings → `advanced/custom_rule_definitions`.
